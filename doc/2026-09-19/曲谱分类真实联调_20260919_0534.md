# 曲谱分类真实联调

2026-09-19 05:34 JST。用户明确允许创建专用测试账号和私有测试数据，并在限流阻塞时提供测试服SSH密钥用于恢复本轮访问。

## 结果

真实目标：`http://163.192.34.11/api`；浏览器访问本地Vite的同源`/api`代理，未模拟API响应。

- 桌面1440×1000完整流程通过，30.1秒；手机390×844完整流程通过，约146秒（降频运行）。
- 真实验证：注册、页面表单登录、无文件pending登记、D/筒音作2→A候选采用、A→G→A有序分段及手动确认、原段ID保留、分类去重/命中段名/whole过滤、ETag与真实412冲突、PNG上传及带Bearer的Blob预览、ABC绑定方案后仍不可跟练、匿名访问私有曲目及附件404、修改原调后仅依赖段needs_review、退出清除身份。
- 两个视口均无未捕获页面异常、无横向溢出。手机视口测试不等于实体手机/麦克风验收。
- 完整通过的桌面测试曲目ID为7、8，手机测试曲目ID为9、10；均保持私有。专用账号用户名和逐项结果见JSON，不包含密码、Bearer令牌或SSH密钥。
- 30项本地模拟浏览器回归通过（17.9秒），生产构建通过。代码和Docs尚未提交、推送或部署。

[结构化结果](曲谱分类真实联调结果_20260919_0534.json) · [桌面截图](曲谱分类真实联调桌面_20260919_0534.png) · [手机截图](曲谱分类真实联调手机_20260919_0534.png)

## 联调发现与修复

1. 后端`NOTATION_NOT_PROVIDED`等原因码原先直接显示，现统一翻译为中文提示，未知内部码采用可理解的待核实说明。
2. 纯文本`403 Access Denied`原先被误提示为“只有创建者可以修改此曲目”。现仅`OWNER_REQUIRED`映射为所有权问题，普通403提示服务拒绝访问，429提示请求过于频繁；添加浏览器回归。
3. 前两轮测试中出现断言过于精确及重复点击已选指法的测试定位问题，已修正，不将中断的流程记为通过。

## 测试服限流恢复

第一次手机测试注册/登录成功后，其余端点（包含公开字典）开始返回403。只读核对确认本轮测试来源IP于UTC20:26:07被以`Rate Limit Exceeded`封禁，计划到期时间UTC21:26:07；本地后端规则为每分钟60次请求，超限封禁60分钟。

经用户提供的SSH连接完成核对，仅将这条匹配来源IP、原因和时间的记录`is_active`设为0，更新行数为1。其他来源的扫描封禁保持原样；未调整限流规则、未添加白名单、未重启服务。公共分类字典随后返回200。

真实测试脚本已对页面与辅助断言的所有API请求统一串行调度，间隔至少2.2秒（低于60次/分钟）；仅延迟原始请求，不修改请求或响应。手机复验完整通过。

## 数据与验收边界

本轮总共创建5个隔离测试账号。两次测试定位修正前保留了4首私有条目；完整通过的桌面、手机各2首，总共8首测试条目。受限流阻塞的手机尝试仅注册账号，没有建成曲目。所有资料为合成测试PNG/ABC，没有上传用户音频或真实曲谱，也没有公开任何条目。当前后端不提供永久删除曲目接口，未绕过接口清理业务表。

仍未执行真实公开/撤回/他人复制流程（这些已有本地模拟回归）；没有验证PCM WAV上传、真实设备音频、A04谱面定位/跟练、正式部署或大规模分页。

## 复验

源码：`tests/live.integration.spec.js`，默认跳过，必须显式启用。它会创建新的隔离测试账号与私有数据，因此只在已授权测试服执行。凭据仅存在运行内存，不保存storageState、trace或视频。临时目录为工作区`.tmp`。

```sh
PLAYWRIGHT_BROWSERS_PATH="$PWD/.playwright-browsers" SOUNDROOM_LIVE_SMOKE=1 npm run test:e2e -- tests/live.integration.spec.js --workers=1
```

本地模拟回归不需要写入测试服：

```sh
PLAYWRIGHT_BROWSERS_PATH="$PWD/.playwright-browsers" npm run test:e2e -- tests/classification.spec.js --workers=2
```
